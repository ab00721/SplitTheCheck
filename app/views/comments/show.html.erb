<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <h2 class="font-bold text-4xl">Viewing Comment</h2>


    <%= render @comment %>

    <!-- <%= link_to "Edit this comment", edit_comment_path(@comment), class: "format-button" %>
      <div class="inline-block ml-2">
        <%= button_to "Destroy this comment", comment_path(@comment), method: :delete, class: "format-button" %>
      </div>
    -->
    <%= link_to "Back to restaurant", restaurant_path(@comment.restaurant.id) , class: "format-button" %>
  </div>
</div>
